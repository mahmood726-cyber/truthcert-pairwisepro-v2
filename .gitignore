# Node
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.venv/
build/
dist/

# Sentinel pre-push hook output (auto-generated per session)
STUCK_FAILURES.md
STUCK_FAILURES.jsonl
sentinel-findings.md
sentinel-findings.jsonl
review-findings.md
review-findings.jsonl

# Session state
PROGRESS.md
DECISIONS.md

# OS
.DS_Store
Thumbs.db

# HTML / JS backup snapshots (local workspace copies, regenerable from
# the canonical TruthCert-PairwisePro-v1.0.html and app.js — don't commit)
*.backup-*.html
*.backup-pre-*.html
*.backup-pre-release
*.html.backup-pre-*
*.js.backup-pre-*

# Recovery snapshots (point-in-time directory copies, reconstructable from git)
recovery_snapshot_*/

# Versioned HTML snapshots (old intermediate versions)
TruthCert_v*-*_FIXED*.html
